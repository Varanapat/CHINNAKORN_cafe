<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>สรุปรายการ</title>
  <link rel="stylesheet" href="/style_confirm_order_customer.css">


</head>
<body>
    <div class="header">
        <a href="/">&#11013;</a>
        <h2>สรุปรายการ ( <%= cart.length %> รายการ )</h2>
    </div>
 
  <div class="container">
    
    <!-- สรุปรายการฝั่งซ้าย -->
    <div class="order-summary">
      <% cart.forEach(item => { %>
      <div class="menu-item">
        <img src="/picture/<%= item.menu_image %>" alt="<%= item.menu_name %>">
        <div class="details">
            <div class="name_order">
                <div class="name"><h3><%= item.menu_name %></h3></div>
                <div class="quantity"><%= item.quantity %> X</div>
            </div>
    
            <ul>
            <% if (item.options && item.options.length > 0) { %>
                <% item.options.forEach(opt => { %>
                <li><%= opt.name %><%= opt.extra && opt.extra > 0 ? ` (+${opt.extra})` : "" %></li>
                <% }) %>
            <% } %>
            </ul>
        
          <div class="price"><%= item.totalPrice_forMenu %> ฿</div>
        </div>
      </div>
      <% }) %>
        <% 
            let beforeVAT = (total * 100) / 107;
            let vat = total - beforeVAT;
        %>


      <div class="total-box_not-final">
        <span>ยอดรวมก่อน vat :</span>
       <span><%= beforeVAT.toFixed(2) %> ฿</span>
      </div> 
      <div class="total-box_not-final">
        <span>vat :</span>
       <span><%= vat.toFixed(2) %> ฿</span>
      </div> 




      <div class="total-box">
        <span>ยอดรวม :</span>
        <span id="total-display"><%= total %> ฿</span>

      </div>
    </div>

    <!-- ฝั่งขวา วิธีการชำระเงิน -->
    <div class="payment">
      <h3>วิธีการชำระเงิน</h3>
      <div class="pay-buttons">
        <a class="qr" href="/pay_qr-customer">QR พร้อมเพย์</a>
      </div>

     <p>ชำระ (<%= total %>)</p>



    
    </div>
 
    </div>


    
  </div>


</body>
</html>
