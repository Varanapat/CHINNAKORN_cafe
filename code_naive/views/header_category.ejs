<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/style_main.css">
</head>
<body>
    <!-- <header>
    <h1>เลือกเครื่องดื่ม</h1>
    <div class="container" id="header">
        <% data.forEach(item => { %>
        <a href="/category/<%= item.category_id %>">
        <div class="cate">
            <%= item.category_name %>
        </div>
        </a>
        <% }); %>
     </div>
    </header> -->


    <header>
        <a href="#" class="logo">CHINNAKORN cafe</a>

        <nav>
            <% data.forEach(item => { %>
            <a href="/category/<%= item.category_id %>" class="active"><%= item.category_name %></a>
            <% }); %>
        </nav>
    </header>
</body>
</html>